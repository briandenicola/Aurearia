# Session: Feature #216 Circular Capture Clip — Full Implementation & Security Hardening

**Date:** 2026-05-31  
**Team:** Cassius (Backend), Aurelia (Frontend), Maximus (Principal Architect), Brutus (QA)  
**Feature:** #216 Circular capture to transparent PNG  
**Status:** ✅ COMPLETED & APPROVED  
**Branch:** `beta`  

## Summary

Completed the full circular capture feature end-to-end: Go primitive for circular clipping with anti-aliased edges (11 tests), FE redesign with overlay focus guide (tiles + soft gradient vignette), handler integration wiring the `circleClip` field, FE cover-crop geometry matching displayed 4:3 region, and critical ownership-before-decode security hardening. Passed Brutus QA gate (14 tests green, build clean) + Maximus architect review (APPROVE). Feature now ready for Brian's on-device visual verification.

## Batch Commits

| Hash | Message | Agent | Details |
|------|---------|-------|---------|
| `0a19708` | feat(#216): add capture.ClipToCircle circular image clip helper | Cassius | Standalone `src/api/capture/` package: `ClipToCircle()` with anti-aliased edge, `ClipBytesToCirclePNG()` encoder, `DefaultGuide` (50%/52% center, 74% width, 360px cap). Stdlib-only (image, image/color, image/png, image/jpeg, math). 11 comprehensive tests (geometry, sizing, transparency, anti-alias, encoding). |
| `234e31c` | feat(#216): redesign capture controls as tiles + circular focus-guide overlay | Aurelia | Redesigned capture UI in AddCoinPage.vue: tiles for camera/gallery/batch, soft gradient vignette focus guide overlay, "Opt" corner badge, primary + ghost link CTAs. Design tokens only (no hardcoded colors/radii). |
| `460441a` | feat(#216): clip obverse/reverse captures to transparent PNG via circleClip flag | Cassius | Wired `circleClip` field into Upload (multipart) and UploadBase64 (JSON) handlers. When true + obverse/reverse: decode, clip via `capture.ClipBytesToCirclePNG()`, store as PNG. Card/detail never clipped. Decode-error fallback: log warning, store original. Architecture test updated to allow `handlers/` → `capture/`. 7 new tests. |
| `df65020` + `e3b3f8d` | feat(#216): cover-crop camera captures and send circleClip for obverse/reverse | Aurelia | Implemented `computeCoverCropRect()` replicating CSS object-fit:cover (4:3 container crop of native video stream). FE draws ONLY the visible region to canvas before upload, ensuring BE's center-fixed `DefaultGuide` produces circle matching on-screen overlay. Threads `circleClip=true` only for camera obverse/reverse. Manual/file-picker unaffected. |
| `5d5df83` | fix(#216): validate coin ownership before decoding/clipping uploads | Cassius | **Security hardening:** Added early `FindCoinByOwner()` check in both Upload/UploadBase64 handlers before file read or base64 decode when `circleClip=true`. Prevents CPU-intensive decode on attacker-specified coin IDs (even if auth'd user tries non-owned coin). Also added early 20MB fail-fast check in multipart path. 2 new non-owner security tests (total 9 clip tests). Resolves Maximus review BLOCK. |

## Review & QA Gate

### Brutus (QA) — VERDICT: ✅ **QA PASS**

**Test Results:**
- 14 tests green (circular clip tests + integration)
- Go build/vet/test ✓
- No blockers

**Advisory flags:**
- Original decode-before-ownership ordering noted as violation of Principle XI. **Status: RESOLVED by commit 5d5df83 (ownership check added)**
- On-device visual verification checklist handed off to Brian:
  - Frame coin in guide → verify clipped output matches guide position with no offset
  - Confirm smooth anti-aliased circle and transparent corners
  - Test portrait + landscape orientation
  - Verify manual gallery uploads stay unclipped

### Maximus (Principal Architect) — VERDICT: ✅ **APPROVE**

**Review Notes:**
- Contract well-defined (hook point, field names, geometry math documented)
- FE cover-crop logic matches CSS object-fit:cover semantics (correct)
- BE DefaultGuide placement (center 50%/52%) sound
- **Non-blocking BLOCK→CLEARED:** Initial implementation decoded bytes before ownership check (Principle XI). Commitment to harden: "Will add early ownership gate before any decode." **Honored in commit 5d5df83.**

## Validation (Summary)

| Tool | Result |
|------|--------|
| `go build ./...` | ✅ Clean |
| `go vet ./...` | ✅ Clean |
| `go test -v ./...` | ✅ All tests pass (architecture + unit + clip + ownership) |
| `npm run lint` | ✅ Clean |
| `npm run build` (vue-tsc + vite) | ✅ Clean, 8.46s |
| Design tokens | ✅ All CSS uses variables.css tokens, no hardcoded values |
| PWA compliance | ✅ Mobile viewport tested, ready for on-device |

## Architecture Compliance

**Principle I (Layered Architecture):**
- Handlers gate ownership before decode
- Service layer unchanged from existing flow
- Clip logic isolated in `src/api/capture/` (stdlib-only utility package)
- Architecture test updated to allow `handlers/` → `capture/` import

**Principle XI (Security Hardening):**
- Input validation ✓ (ownership pre-check, 20MB early fail-fast)
- Output encoding ✓ (PNG with alpha channel, no text content)
- Decode safety ✓ (ownership gate before CPU-intensive operations)
- Decode-error fallback ✓ (log + continue with original, never 500)

**§17 Quality Gate:**
- All tests passing
- Build clean (go + vue)
- Conventional commit + trailer applied
- Decision recorded in decisions.md (#21)

## Outstanding Work (User Responsibility)

**On-device visual checks (Brian):**
1. Capture a coin with camera (frame it in the guide)
2. Verify the clipped PNG matches the on-screen guide position (no offset)
3. Confirm smooth anti-aliased circle with transparent corners
4. Test in both portrait and landscape orientation
5. Upload a coin via manual gallery picker and verify it remains unclipped (rectangular JPEG/PNG)

## Decision Recorded

**Decision #21:** Feature #216 Circular Capture Clip — Integration Contract & Implementation Complete  
**Status:** APPROVED & LANDED  
**Location:** `.squad/decisions.md` (merged from inbox)

---

**Feature #216 now complete.** All code landed, reviewed, hardened, and tested. Ready for on-device validation and merge to main.
