# Orchestration Log: Cassius (Issue #218 — Phase 2 & 3)

**Agent:** Cassius  
**Date:** 2026-06-01T12:59:24Z  
**Outcome:** DONE  
**Related:** Issue #218 (External Tool Server Adapter)

## Tasks Completed

**Phase 2 (Foundational Infrastructure):**
- T003–T005: Capability model (string-based, `"read"` or `"read,write"` on ApiKey)
- T006, T009: Admin kill-switch setting (`SettingExternalToolServerEnabled`, default false)
- T007: API-key auth context extension (`apiKeyCapabilities`, `apiKeyId`, `apiKeyName`)
- T008: Capability enforcement middleware (`RequireCapability("read"/"write")`)
- T010: Per-key external rate limiter (`ExternalAPIKeyRateLimit(50, 1*time.Minute)`)
- T011: Public route group registration (`/api/v1/tools` with middleware chain)

**Phase 3 (Handlers & Routes):**
- T012–T014: Read handlers (SearchMyCollection, GetCoin, CollectionSummary, TopCoinsByValue)
- T015: Journal-source threading (CommitProposal → buildJournalEntry with source + metadata)
- T016–T018: Write handlers (ProposeUpdate, CommitUpdate with API key metadata)
- T019–T020: Served OpenAPI spec (embedded YAML → JSON at `/api/v1/tools/openapi.json`)
- T021: API key scope parameter in handler (optional `scope` field, defaults to `"read"`)

## Validation Results

```bash
cd src/api/
go build ./...  # ✅ clean
go vet ./...    # ✅ clean
go test ./...   # ✅ all tests pass
```

## Key Decisions Recorded

- **Capability Model:** String-based normalized representation on `ApiKey` field, helpers `HasRead()` / `HasWrite()`
- **Kill Switch:** Middleware `ExternalToolServerEnabled` returns 503 when setting is not `"true"`
- **Middleware Chain:** Gate (kill switch) → Auth → Per-key Rate Limit, applied uniformly to `/api/v1/tools`
- **Journal-Source Threading:** `CommitUpdate()` (internal) vs `CommitUpdateExternal()` (external) routes to shared `commitProposalWithSource(source, metadata)`
- **OpenAPI Spec:** Embedded via `go:embed`, served as JSON (not YAML) via `gopkg.in/yaml.v3` parser

## Handoff

All code landed; handlers ready for integration with in-app collection chat share. Phase 3 (handlers) complete pending review gate (Maximus). No unresolved blockers except **Strict Lockout review gate.**

---

**See:** `.squad/decisions/inbox/cassius-218-foundational.md`, `.squad/decisions/inbox/cassius-218-handlers.md`
