## Post-Major-Work QC Audit — Feature 358 Structured Storage Trays

**Audited by:** GitHub Copilot CLI  
**Date:** 2026-09-11  
**Changeset:** `git diff HEAD` plus untracked files on `feat/structured-storage-trays`  
**Spec / design doc:** `specs/358-structured-storage-trays/spec.md`

### Scope Summary

Feature 358 adds additive storage-location and coin-slot schema changes, owner-scoped tray APIs, shared assignment validation, settings and coin-form workflows, and a fixed-geometry authenticated tray view. The audit covered backend, frontend, migration, generated API contracts, documentation, and tests; no dependency, container, workflow, Python-agent, or runtime configuration changes were introduced.

### Artifact Checklist

- [x] Diff read (all changed and untracked files)
- [x] Authorizing spec, plan, research, data model, and UI/API contracts read
- [x] Relevant decisions read; no new ADR was required
- [x] CI workflows and pull-request Definition of Done read
- [x] Constitution hierarchy, §17 Quality Gate, and §21 Definition of Done reviewed
- [x] All eight audit domains traversed

### Blockers

None. PR #702 passed the complete GitHub Quality Gate, including the Linux race-detector job.

### Follow-Ups

| ID | Domain | File : Line | Finding | Suggested Remediation |
|----|--------|-------------|---------|----------------------|
| F1 | Test Reliability | `src/api/services/pushover_service_test.go:382-460` | Auction countdown fixtures use separate `time.Now()` calls and can cross a truncation boundary on Windows. This test is unrelated to Feature 358 and passed in the authoritative Linux CI run. | Stabilize the test separately with an injected clock or boundary-safe duration assertion. |

### Findings Resolved During Audit

| ID | Domain | File : Line | Resolution |
|----|--------|-------------|------------|
| R1 | Accessibility / Contract | `src/web/src/components/tray/StorageTrayGrid.vue:8-18` | Added the required name to the actual `role="grid"`, including tray name, dimensions, and occupancy; component and Playwright assertions now enforce it. |
| R2 | Migration Safety | `src/api/database/feature358_migration_regression_test.go:27-83` | Expanded the legacy fixture to preserve explicit timestamps and two coins assigned to the same Standard Location across repeated migration runs. |
| R3 | API Documentation | `src/api/handlers/coins.go:360-372` | Clarified that explicit `storageSlot: null` is accepted only when the resulting location/slot combination is valid, then regenerated synchronized OpenAPI artifacts. |
| R4 | Design System | `src/web/src/components/CoinForm.vue:570-608` | Replaced the unsupported `0.65rem` slot-state text with the approved `0.75rem` tiny-text size. |
| R5 | Browser Workflow | `src/web/src/pages/AddCoinPage.vue:456-486` | Restored `mintLocationId` propagation in manual-create payloads; the deterministic add workflow now verifies the linked mint ID. |
| R6 | Browser Test Fidelity | `src/web/e2e/workflows/coin-form.spec.ts:154-188` | Replaced a stale `.hero-slot` selector with the rendered Obverse/No image relationship. |

### Positive Observations

- Repository transactions and the named partial unique index make slot exclusivity authoritative while preserving the previous assignment on a failed move.
- New occupancy and aggregate routes remain inside the authenticated route group and consistently apply owner scopes and generic cross-owner 404 behavior.
- The migration is additive and idempotent, keeps legacy locations Standard by default, preserves IDs and assignments, and fails visibly on malformed duplicate slots or case-insensitive names.
- The physical tray view reuses shared felt/well/media primitives while keeping Museum Tray packing, drawers, preferences, and responsive behavior separate.
- Automated coverage spans migration, repository, service, handler, sibling write workflows, Vue components, accessibility, and focused Playwright workflows.

### Validation Evidence

- Passed: `go build ./...`
- Passed: `go vet ./...`
- Passed: `go test ./...` in the PR #702 GitHub Quality Gate
- Passed: focused Feature 358 database, repository, service, handler, and workflow tests
- Passed: GitHub Linux `Go API (race detector)` job on PR #702
- Passed: frontend ESLint, `vue-tsc --build`, Vitest, and production Vite/PWA build
- Passed: all 14 targeted `storage-trays`, `coin-form`, and Museum Tray Playwright tests
- Passed: regenerated Swagger/OpenAPI synchronization and `git diff --check`
- Passed: locked constitution, spec, decisions, agent charter, and append-only log files remain unchanged

### Confidence Notes

Manual screen-reader testing was not performed. Automated accessible-name, keyboard activation, mobile overflow, missing-image, and retry-path tests passed, and PR #702 completed the full Linux CI and security gates before merge.
